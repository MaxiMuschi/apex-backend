# Deploy The Study Journal (static site) to GitHub Pages.
# After the first run, enable Pages in repo Settings → Pages → Source: "GitHub Actions".

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Assemble site
        # The site is already static; copy the files Pages should serve into ./_site.
        # index.html is the brand landing page; app.html is the learning app.
        run: |
          mkdir -p _site
          cp index.html app.html styles.css app.js boot.js site.js config.js curriculum.js apex-logo.svg _site/
          # _headers is Netlify-specific but harmless to include.
          cp _headers _redirects _site/ 2>/dev/null || true
          # README is handy at the root of the published site too.
          cp README.md _site/ 2>/dev/null || true

      - name: Sanity check JS
        run: |
          node --check _site/app.js
          node --check _site/boot.js
          node --check _site/site.js
          node --check _site/config.js
          node --check _site/curriculum.js

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
